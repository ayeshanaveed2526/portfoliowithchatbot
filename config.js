// Configuration file for application settings
// API keys are now handled securely via Vercel Serverless Functions
const CONFIG = {
    API_ENDPOINT: '/api/chat' // Local API endpoint for production
};
